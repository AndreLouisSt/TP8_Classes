#include <vector>
#include <algorithm>
#include <iterator>

using namespace std;

int main() {
    vector<int> vecteurE (3);
    vecteurE.push_back(1);
    vecteurE.push_back(2);
    vecteurE.push_back(3);
}